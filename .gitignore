# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — NEVER commit real secrets
.env
.env*.local
.env.production

# server settings holding the admin secure code
settings.json

# uploaded files live on local disk
/uploads/*
!/uploads/.gitkeep

# local backups produced by scripts/backup.sh
/backups

# claude code local (machine-specific permissions)
.claude/settings.local.json

# typescript
*.tsbuildinfo

# drizzle
/drizzle/meta
